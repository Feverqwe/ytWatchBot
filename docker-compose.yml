{
  "version": "2",
  "services": {
    "bot": {
      "build": ".",
      "ports": [
        "1337:1337"
      ],
      "volumes": [
          "./log:/opt/backend/log"
      ],
      "restart": "always",
      "network_mode": "host"
    }
  }
}