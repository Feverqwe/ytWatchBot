{
  "version": "2",
  "services": {
    "ytwatchbot": {
      "build": ".",
      "ports": [
        "1337:1337"
      ],
      "volumes": [
          "./log:/opt/ytWatchBot/log"
      ],
      "restart": "always",
      "network_mode": "host"
    }
  }
}